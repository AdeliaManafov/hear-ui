name: Generate Client (Archived)

# Archived for MVP: client generation workflow replaced with noop to avoid regenerating client during PRs
on: [workflow_dispatch]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Generate-client workflow archived for MVP (2025-11-19)"
