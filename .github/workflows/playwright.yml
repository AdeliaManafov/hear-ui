name: Playwright Tests (Archived)

# Archived for MVP: heavy Playwright E2E workflow replaced with noop to reduce CI time
on: [workflow_dispatch]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Playwright workflow archived for MVP (2025-11-19)"
