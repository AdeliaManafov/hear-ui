# frontend/src/hooks

Custom React Hooks, z. B. für Auth, Formulare oder API-Aufrufe (TanStack Query Integration).

Konvention:
- Hooks beginnen mit `use` (z. B. `useAuth`, `useFetchItems`).
- Dokumiere erwartete Inputs/Outputs in der Datei oder in einer zentralen `docs/`-Datei.
