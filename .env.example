# .env.example (secure template for HEAR MVP)
# Copy to .env and fill in real values. DO NOT commit .env to git.

# Environment
ENVIRONMENT=development
FRONTEND_HOST=http://localhost:5173
SENTRY_DSN=

# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_strong_password
POSTGRES_DB=hear_db

# Application / Admin
EMAILS_FROM_EMAIL=admin@example.com
FIRST_SUPERUSER_EMAIL=admin@example.com
FIRST_SUPERUSER_PASSWORD=ChangeMe123!

# Security
# Generate a secure SECRET_KEY and paste it here:
# python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=replace-with-secure-random-value

# Optional: production DB URL (if you prefer a single var)
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# Notes:
# - After copying .env.example -> .env, set file permissions: chmod 600 .env
# - In CI / production, store secrets in the CI secret store (GitHub Actions secrets, Docker secrets, etc.)
# Example .env file for HEAR project
# Copy to .env and fill in real values (DO NOT commit .env)

# API / frontend
FRONTEND_HOST=http://localhost:5173
ENVIRONMENT=local
SENTRY_DSN=

# Database
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me
POSTGRES_DB=hear_db

# Email / Admin
EMAILS_FROM_EMAIL=admin@example.com
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=ChangeMe123!

# Security
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=replace-with-secure-random-value
