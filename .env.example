# Example .env.example for HEAR project
# Copy this file to `.env` and fill in secure values for local development.
# This file is safe to commit and documents the variables used by the project.

# Docker stack and domains
STACK_NAME=hear-ui
DOMAIN=localhost

# Docker image tags
DOCKER_IMAGE_BACKEND=hear-backend
DOCKER_IMAGE_FRONTEND=hear-frontend
TAG=local

# Postgres (canonical single block)
# When running with Docker Compose use POSTGRES_SERVER=db (container)
POSTGRES_SERVER=db
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=hear_db

# App / runtime
ENVIRONMENT=local
FRONTEND_HOST=http://localhost:5173
BACKEND_CORS_ORIGINS=http://localhost:5173

# Security / bootstrap
SECRET_KEY=replace-with-secure-random-value
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=ChangeMe123!

# Email (optional)
# (removed for MVP — not required)

# Observability
SENTRY_DSN=

# Notes:
# - Keep a single canonical set of POSTGRES_* vars (Server, Port, User, Password, DB).
# - If running backend locally (not in Docker), set POSTGRES_SERVER=127.0.0.1 and
#   POSTGRES_PORT to the host port where the DB is exposed (e.g. 5433).
# - Protect this file (chmod 600) if it contains real secrets.
