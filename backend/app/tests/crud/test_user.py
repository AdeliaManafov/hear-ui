import pytest

pytestmark = pytest.mark.skip(reason="Auth tests archived in `archiviert/backend_auth/`")

# Auth CRUD tests archived; module is intentionally skipped to avoid importing removed auth helpers.
