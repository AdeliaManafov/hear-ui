import pytest

pytestmark = pytest.mark.skip(reason="Auth endpoints archived in `archiviert/backend_auth/`")

# Auth tests archived; module is intentionally skipped to avoid importing removed auth helpers.
