# frontend/src/client

Generierter OpenAPI-Client für den Zugriff auf die Backend-API.

Wichtig:
- Bei Änderungen am Backend-OpenAPI-Schema: Client neu generieren (`./scripts/generate-client.sh` oder `npm run generate-client`).
- Dieser Code ist generiert — vermeide manuelle Änderungen oder dokumentiere sie deutlich.
